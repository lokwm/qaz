//整体代码写的比较简单,主要是根据定义来写的
#include<stdio.h>
int main()
{
	int n,m,p;
	printf("Input n m p:(p>m,n)");
	scanf("%d%d%d",&n,&m,&p);
    if(p%(n-m)==0)
		printf("n m p满足同余定理\n");
	else
		printf("n m p不满足同余定理\n");
	return 0;
}
